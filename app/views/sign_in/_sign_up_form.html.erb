<%= form_for :users, url: users_path, method: :post, html: { id: 'sign-up_form', class: 'form' } do |form| %>
  <div class="text-field form-item">
    <%= form.label :email %>
    <%= form.text_field :email, type: 'email', class: 'input' %>
  </div>

  <div class="password-field form-item">
    <%= form.label :password %>
    <%= form.password_field :password, class: 'input' %>
  </div>

  <div class="submit-field form-item">
    <%= form.submit 'Sign up', class: 'btn' %>
  </div>
<% end %>